import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.timeywimey.app',
  appName: 'timey-wimey',
  webDir: 'dist/timey-wimey/browser'
};

export default config;
